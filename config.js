// Configuration file for Weather App
// This file can be used as an alternative way to set your API key

const CONFIG = {
    // Replace 'YOUR_API_KEY_HERE' with your actual OpenWeatherMap API key
    API_KEY: 'YOUR_API_KEY_HERE',
    
    // API configuration
    BASE_URL: 'https://api.openweathermap.org/data/2.5',
    
    // Default settings
    DEFAULT_UNIT: 'celsius', // 'celsius' or 'fahrenheit'
    
    // App settings
    APP_NAME: 'Weather App',
    VERSION: '1.0.0'
};

// Export for use in other files (if using modules)
// export default CONFIG;

// For direct script inclusion, CONFIG will be available globally
